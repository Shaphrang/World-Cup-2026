"use client";
import { useEffect, useState } from "react"; import Swal from "sweetalert2"; import { DataTable } from "@/components/admin/DataTable"; import { LoadingSpinner } from "@/components/admin/LoadingSpinner"; import { supabase } from "@/lib/supabaseClient"; import { friendlyError } from "@/lib/supabaseHelpers"; import type { LeaderboardRow } from "@/lib/types";
export default function Leaderboard(){ const [rows,setRows]=useState<LeaderboardRow[]|null>(null); useEffect(()=>{supabase.from('leaderboard_view').select('*').then(({data,error})=>{if(error)Swal.fire('Load failed',friendlyError(error),'error'); setRows((data??[]) as LeaderboardRow[])})},[]); if(!rows)return <LoadingSpinner/>; return <section className="space-y-5"><div><h1 className="text-2xl font-bold">Leaderboard</h1><p className="text-sm text-slate-500">Ranking from leaderboard_view.</p></div><DataTable data={rows.map((r,i)=>({id:String(r.user_id??r.profile_id??i),...r}))} columns={[{header:'Rank',accessor:r=>r.rank??'—'},{header:'Name',accessor:r=>r.full_name??r.email??'—'},{header:'Points',accessor:r=>r.total_points??0},{header:'Correct Scores',accessor:r=>r.correct_scores??0},{header:'Correct Scorers',accessor:r=>r.correct_scorers??0}]}/></section> }
