"use client"; import { CrudPage } from "@/components/admin/CrudPage"; import type { SponsorBanner } from "@/lib/types";
export default function Banners(){ return <CrudPage<SponsorBanner> title="Sponsor Banners" table="sponsor_banners" columns={[{header:'Title',accessor:'title'},{header:'Active',accessor:r=>r.is_active?'Yes':'No'},{header:'Sort',accessor:'sort_order'}]} fields={[{name:'title',label:'Title',required:true},{name:'image_url',label:'Image URL',type:'url',required:true},{name:'target_url',label:'Target URL',type:'url'},{name:'sort_order',label:'Sort Order',type:'number'},{name:'is_active',label:'Active',type:'checkbox'}]} initialRecord={{is_active:true,sort_order:0}}/> }
