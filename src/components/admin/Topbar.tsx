"use client";
import { supabase } from "@/lib/supabaseClient"; import { LogOut, ShieldCheck } from "lucide-react"; import { useRouter } from "next/navigation"; import Swal from "sweetalert2";
export function Topbar({ email }: {email?: string|null}){ const router=useRouter(); async function logout(){ await supabase.auth.signOut(); await Swal.fire('Signed out','You have been logged out.','success'); router.replace('/login'); } return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="flex h-16 items-center justify-between px-4 sm:px-6"><div><h1 className="font-semibold text-slate-950">Administration</h1><p className="text-xs text-slate-500">Secure operations console</p></div><div className="flex items-center gap-3"><div className="hidden items-center gap-2 rounded-full bg-emerald-50 px-3 py-1.5 text-sm text-emerald-800 sm:flex"><ShieldCheck className="h-4 w-4"/>{email}</div><button onClick={logout} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-3 py-2 text-sm font-medium text-white hover:bg-slate-800"><LogOut className="h-4 w-4"/>Logout</button></div></div></header> }
